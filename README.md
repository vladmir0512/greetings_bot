# Greetings Bot

Telegram-бот для приёма заявок в комьюнити с автоматической синхронизацией данных в Google Sheets.

## Возможности

- 📝 Интерактивный опрос для новых участников
- 💾 Сохранение заявок в локальную базу данных SQLite
- 👨‍💼 Админ-панель для рассмотрения заявок
- ✅ Автоматическая синхронизация одобренных заявок в Google Sheets
- 🔗 Отправка ссылки на вступление при одобрении
- 📊 История всех заявок от одного пользователя
- 🚫 Защита от дублирования заявок

## Требования

- Python 3.8+
- Telegram Bot Token
- Google Cloud проект с включённым Google Sheets API
- Сервисный аккаунт Google Cloud с доступом к таблице

## Установка

1. Клонируйте репозиторий или скачайте файлы проекта

2. Создайте виртуальное окружение:
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:
   - Windows: `venv\Scripts\activate`
   - Linux/Mac: `source venv/bin/activate`

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

5. Создайте файл `.env` в корне проекта со следующим содержимым. Как пример использовать .env.txt .
```

## Настройка

### Получение токена бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env`

### Настройка Google Sheets

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Включите Google Sheets API в разделе "APIs & Services" → "Library"
3. Создайте сервисный аккаунт:
   - Перейдите в "IAM & Admin" → "Service accounts"
   - Нажмите "Create service account"
   - Скачайте JSON-ключ (это будет `service_account.json`)
4. Откройте вашу Google таблицу и нажмите "Поделиться"
5. Добавьте email сервисного аккаунта (из `client_email` в JSON) с правами "Редактор"
6. Скопируйте ID таблицы из URL (часть между `/d/` и `/edit`) в `.env`

### Получение Telegram ID администратора

1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Скопируйте ваш ID в переменную `ADMIN_IDS` в `.env` (через запятую для нескольких админов)

## Использование

### Запуск бота

```bash
python bot.py
```

### Команды бота

- `/start` - Начать подачу заявки или проверить статус
- `/status` - Проверить статус последней заявки
- `/cancel` - Отменить текущий опрос
- `/admin` - Админ-панель (только для администраторов)

### Админ-панель

Администраторы могут:
- Просматривать список заявок на рассмотрение
- Видеть историю всех заявок от пользователя
- Одобрять или отклонять заявки
- Автоматически выгружать одобренные заявки в Google Sheets

### Синхронизация данных

Одобренные заявки автоматически добавляются в Google Sheets со следующими полями:
- ID (Telegram User ID)
- Имя
- Username
- Город
- Опыт
- Цель

Для ручной синхронизации существующих заявок используйте:
```bash
python sync_to_sheet.py
```

## Структура проекта

```
greetings_bot/
├── bot.py                 # Основной файл бота
├── config.py              # Конфигурация и настройки
├── db.py                  # Работа с базой данных
├── google_client.py       # Интеграция с Google Sheets
├── sync_to_sheet.py       # Скрипт для синхронизации
├── requirements.txt       # Зависимости проекта
├── .env                   # Переменные окружения (создайте сами)
├── service_account.json   # Ключ сервисного аккаунта Google
└── data/                  # Папка с базой данных
    └── applications.db
```

## Лицензия

MIT License - см. файл [LICENSE](LICENSE) для деталей.

## Поддержка

При возникновении проблем проверьте:
- Правильность заполнения `.env`
- Доступность Google Sheets API
- Права сервисного аккаунта на таблицу
- Логи бота в консоли

